import { Span } from "@opentelemetry/sdk-trace-base";
import { GlobalLogger as Logger } from "./telemetry/logs";
import { WorkflowContextImpl } from "./workflow";
import { DBOSContext, DBOSContextImpl } from "./context";
import { WorkflowContextDebug } from "./debugger/debug_workflow";
export type StepFunction<T extends unknown[], R> = (ctxt: StepContext, ...args: T) => Promise<R>;
export interface StepConfig {
    retriesAllowed?: boolean;
    intervalSeconds?: number;
    maxAttempts?: number;
    backoffRate?: number;
}
export interface StepContext extends DBOSContext {
    readonly retriesAllowed: boolean;
    readonly maxAttempts: number;
}
export declare class StepContextImpl extends DBOSContextImpl implements StepContext {
    readonly functionID: number;
    readonly retriesAllowed: boolean;
    readonly intervalSeconds: number;
    readonly maxAttempts: number;
    readonly backoffRate: number;
    constructor(workflowContext: WorkflowContextImpl | WorkflowContextDebug, functionID: number, span: Span, logger: Logger, params: StepConfig, commName: string);
}
//# sourceMappingURL=step.d.ts.map