import { DBOSConfig } from "../dbos-executor";
import { PoolConfig } from "pg";
import YAML from "yaml";
import { DBOSRuntimeConfig } from "./runtime";
import { UserDatabaseName } from "../user_database";
import { TelemetryConfig } from "../telemetry";
export declare const dbosConfigFilePath = "dbos-config.yaml";
export interface ConfigFile {
    name?: string;
    language?: string;
    database: {
        hostname: string;
        port: number;
        username: string;
        password?: string;
        connectionTimeoutMillis?: number;
        app_db_name: string;
        sys_db_name?: string;
        ssl?: boolean;
        ssl_ca?: string;
        app_db_client?: UserDatabaseName;
        migrate?: string[];
        rollback?: string[];
        local_suffix: boolean;
    };
    http?: {
        cors_middleware?: boolean;
        credentials?: boolean;
        allowed_origins?: string[];
    };
    telemetry?: TelemetryConfig;
    application: object;
    env: Record<string, string>;
    runtimeConfig?: DBOSRuntimeConfig;
}
export declare function substituteEnvVars(content: string): string;
/**
 * Loads config file as a ConfigFile.
 * @param {string} configFilePath - The path to the config file to be loaded.
 * @returns
 */
export declare function loadConfigFile(configFilePath: string): ConfigFile;
/**
 * Writes a YAML.Document object to configFilePath.
 * @param {YAML.Document} configFile - The config file to be written.
 * @param {string} configFilePath - The path to the config file to be written to.
 */
export declare function writeConfigFile(configFile: YAML.Document, configFilePath: string): void;
export declare function retrieveApplicationName(configFile: ConfigFile): string;
export declare function constructPoolConfig(configFile: ConfigFile): PoolConfig;
export interface ParseOptions {
    port?: number;
    loglevel?: string;
    configfile?: string;
    appDir?: string;
    appVersion?: string | boolean;
}
export declare function parseConfigFile(cliOptions?: ParseOptions, useProxy?: boolean): [DBOSConfig, DBOSRuntimeConfig];
//# sourceMappingURL=config.d.ts.map