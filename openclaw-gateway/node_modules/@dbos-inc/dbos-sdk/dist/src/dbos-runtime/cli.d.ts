#!/usr/bin/env node
import { ConfigFile } from "./config";
import { GlobalLogger } from "../telemetry/logs";
export interface DBOSCLIStartOptions {
    port?: number;
    loglevel?: string;
    configfile?: string;
    appDir?: string;
    appVersion?: string | boolean;
}
export interface DBOSConfigureOptions {
    host?: string;
    port?: number;
    username?: string;
}
export declare function runAndLog(action: (configFile: ConfigFile, logger: GlobalLogger) => Promise<number> | number): Promise<void>;
//# sourceMappingURL=cli.d.ts.map